// Package archive implements the append-only SQLite-backed store of raw
// (still encrypted) telegrams, including transfer between a collector and an
// evaluator and migration of stored data across schema versions.
package archive
