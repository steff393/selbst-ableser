// Package collector is the module root for saCollector: a small,
// standalone agent that reads wM-Bus telegrams and reports them to an
// evaluator. See cmd/saCollector for the entry point; this file exists so
// the module has a root package to hang a whole-module boundary test on
// (see boundary_test.go).
package collector
